buildscript {
    ext {
        // Android Installation Step 1a: Set compatible minSdkVersion and compileSdkVersion
        minSdkVersion = 24 // Minimum supported by Klaviyo Android SDK
        targetSdkVersion = 35 // Typically minSdkVersion <= targetSdkVersion <= compileSdkVersion
        compileSdkVersion = 35 // Klaviyo Android SDK is compiled against 35
        buildToolsVersion = "35.0.0"
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.0.21"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")

        // Android Installation Step - Required dependency to initialize firebase via google-services.json file
        classpath("com.google.gms:google-services:4.4.2")
    }
}

apply plugin: "com.facebook.react.rootproject"
